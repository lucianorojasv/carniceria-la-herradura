import React,{useEffect,useState} from 'react';import {api,money} from '../services/api';import {PageHeader,Loading,Status} from '../components/UI';
export default function Dashboard(){const [d,setD]=useState(null);useEffect(()=>{api('/dashboard').then(setD)},[]);if(!d)return <Loading/>;const cards=[['Ventas de hoy',money(d.salesToday),'💰'],['Pedidos pendientes',d.pendingOrders,'🧾'],['En preparación',d.preparingOrders,'🔪'],['Clientes',d.totalCustomers,'👥'],['Productos',d.totalProducts,'🥩'],['Stock bajo',d.lowStockProducts,'⚠️']];return <><PageHeader title="Dashboard" subtitle="Resumen de la operación digital"/><div className="cards">{cards.map(c=><div className="metric" key={c[0]}><span>{c[2]}</span><div><small>{c[0]}</small><strong>{c[1]}</strong></div></div>)}</div><div className="panel"><h2>Pedidos recientes</h2><div className="table-wrap"><table><thead><tr><th>Código</th><th>Cliente</th><th>Estado</th><th>Total</th><th>Fecha</th></tr></thead><tbody>{d.recentOrders.map(o=><tr key={o.id}><td><b>{o.code}</b></td><td>{o.customer}</td><td><Status value={o.status}/></td><td>{money(o.total)}</td><td>{new Date(o.createdAt).toLocaleString('es-PE')}</td></tr>)}</tbody></table></div></div></>}
