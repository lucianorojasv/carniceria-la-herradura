import React,{useEffect,useState} from 'react';import {api,money} from '../services/api';import {PageHeader,Modal,ErrorBox} from '../components/UI';
const empty={categoryId:'',name:'',description:'',pricePerUnit:'',unit:'KG',stockQuantity:'',minimumQuantity:'0.5',imageUrl:'',active:true,featured:false};
export default function Products(){const [products,setProducts]=useState([]),[categories,setCategories]=useState([]),[form,setForm]=useState(null),[error,setError]=useState('');const load=()=>Promise.all([api('/products'),api('/categories')]).then(([p,c])=>{setProducts(p);setCategories(c)});useEffect(()=>{load()},[]);function edit(p){setForm(p?{...p,categoryId:p.category.id}:{...empty,categoryId:categories[0]?.id||''})}async function save(e){e.preventDefault();setError('');try{const body={...form,categoryId:Number(form.categoryId),pricePerUnit:Number(form.pricePerUnit),stockQuantity:Number(form.stockQuantity),minimumQuantity:Number(form.minimumQuantity)};await api('/products'+(form.id?'/'+form.id:''),{method:form.id?'PUT':'POST',body:JSON.stringify(body)});setForm(null);load()}catch(e){setError(e.message)}}return <><PageHeader title="Productos" subtitle="Precios, stock y catálogo" action={<button className="primary" onClick={()=>edit(null)}>+ Nuevo producto</button>}/><div className="panel"><div className="table-wrap"><table><thead><tr><th>Producto</th><th>Categoría</th><th>Precio</th><th>Stock</th><th>Destacado</th><th></th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td><b>{p.name}</b><small>{p.description}</small></td><td>{p.category?.name}</td><td>{money(p.pricePerUnit)} / {p.unit==='KG'?'kg':p.unit}</td><td>{p.stockQuantity}</td><td>{p.featured?'⭐':'—'}</td><td><button className="ghost" onClick={()=>edit(p)}>Editar</button></td></tr>)}</tbody></table></div></div>{form&&<Modal title={form.id?'Editar producto':'Nuevo producto'} onClose={()=>setForm(null)}><form className="form-grid" onSubmit={save}><label>Categoría<select value={form.categoryId} onChange={e=>setForm({...form,categoryId:e.target.value})}>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Nombre<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label className="full">Descripción<textarea value={form.description||''} onChange={e=>setForm({...form,description:e.target.value})}/></label><label>Precio<input type="number" step="0.01" required value={form.pricePerUnit} onChange={e=>setForm({...form,pricePerUnit:e.target.value})}/></label><label>Stock<input type="number" step="0.001" required value={form.stockQuantity} onChange={e=>setForm({...form,stockQuantity:e.target.value})}/></label><label>Cantidad mínima<input type="number" step="0.001" value={form.minimumQuantity} onChange={e=>setForm({...form,minimumQuantity:e.target.value})}/></label><label>Unidad<select value={form.unit} onChange={e=>setForm({...form,unit:e.target.value})}><option>KG</option><option>UNIT</option><option>PACK</option></select></label><label className="check"><input type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/>Activo</label><label className="check"><input type="checkbox" checked={form.featured} onChange={e=>setForm({...form,featured:e.target.checked})}/>Destacado</label><ErrorBox message={error}/><div className="form-actions full"><button type="button" className="ghost" onClick={()=>setForm(null)}>Cancelar</button><button className="primary">Guardar</button></div></form></Modal>}</>}
