import React,{useState} from 'react';import {NavLink} from 'react-router-dom';import {useAuth} from '../context/AuthContext';
const items=[['📊','Dashboard','/dashboard'],['🧾','Pedidos','/pedidos'],['🥩','Productos','/productos'],['👥','Clientes','/clientes'],['🛵','Delivery','/delivery'],['🔥','Promociones','/promociones'],['🤖','Asistente','/asistente'],['⚙️','Configuración','/configuracion']];
export default function Layout({children}){const {user,logout}=useAuth();const [open,setOpen]=useState(false);return <div className="app-shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand"><img src="/logo.png"/><div><strong>La Herradura</strong><small>Panel de ventas</small></div></div><nav>{items.map(([i,n,p])=><NavLink key={p} to={p} onClick={()=>setOpen(false)} className={({isActive})=>isActive?'active':''}><span>{i}</span>{n}</NavLink>)}</nav><a className="catalog-link" href="/catalogo" target="_blank">🌐 Ver catálogo público</a><button className="logout" onClick={logout}>Cerrar sesión</button></aside><main><header className="topbar"><button className="menu-button" onClick={()=>setOpen(!open)}>☰</button><div><b>{user?.fullName}</b><small>{user?.role}</small></div></header><section className="content">{children}</section></main></div>}
